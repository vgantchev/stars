= simple_form_for(@interesting_object) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :photo, as: :file

  .form-actions
    = f.button :submit, class: "btn btn-md btn-success"
